{
  "name": "project_seed",
  "version": "1.0.0",
  "description": "A community for developers to buld amazing projects",
  "main": "server.js",
  "engines": {
    "node": "v11.6.0"
  },
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd client && npm start",
    "dev": "concurrently --names \"server,client\" \"npm run server --silent\" \"npm run client --silent\"",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Rahat, Ashraful, Beth, Jason",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^1.3.6",
    "bcrypt": "^3.0.5",
    "body-parser": "^1.18.3",
    "concurrently": "^3.5.1",
    "cors": "^2.8.4",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "graphql": "^0.13.2",
    "graphql-tools": "^3.0.2",
    "jsonwebtoken": "^8.2.1",
    "mongoose": "^5.7.5"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
